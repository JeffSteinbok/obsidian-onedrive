# Allow installing eslint-plugin-obsidianmd (requires eslint >=9) alongside
# our project's eslint 8. The plugin uses its own nested eslint 10 for the
# community validator; our existing eslint 8 tooling is unaffected.
legacy-peer-deps=true
